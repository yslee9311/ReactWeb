import React from 'react';
import Receives from './Receives';
import '../index.css'

const RightPane = () => {
    return (
        <div className="pane">
            <Receives />
        </div>
    );
};

export default RightPane;